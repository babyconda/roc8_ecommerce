"use client";
import React from "react";
import { ClockLoader } from "react-spinners";

export default function Spinner() {
  return (
    <>
      <ClockLoader size={50} color="rgba(0, 0,0)" />
    </>
  );
}
